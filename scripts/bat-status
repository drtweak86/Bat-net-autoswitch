#!/usr/bin/env bash
set -euo pipefail
echo "🦇 Bat-Net status"
echo "-----------------"
iw dev wlan0 link || true
iw dev wlan0 station dump 2>/dev/null | awk '/tx bitrate:/ {print}'
echo
ping -c1 -W1 1.1.1.1 | tail -n2 || true
echo
tail -n 25 /home/osmc/wifi_autoswitch.log 2>/dev/null || echo "(no log yet)"
